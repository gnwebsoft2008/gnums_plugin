/// This file contains the dependencies required for the Flutter project.
/// Add these dependencies in the `pubspec.yaml` file of your project.
/// Once added, run the following command to install the dependencies:
/// flutter pub get

get: ^4.6.6                                  # State management solution for Flutter.
path_provider: any                           # Provides platform-specific paths to store files.
progress_dialog_null_safe: ^1.0.7            # Displays progress dialogs in a null-safe manner.
pretty_dio_logger: ^1.2.0-beta-1             # Logger for Dio API requests and responses.
dio: any                                     # HTTP client for Dart to handle RESTFul API calls.
connectivity_plus: any                       # Checks the network connectivity status of the device.
provider: any                                # A wrapper around InheritedWidget for dependency injection.
shared_preferences: any                      # Stores key-value pairs persistently on the device.
sizer: ^2.0.15                               # A utility to help with responsive design based on screen size.
intl: ^0.19.0                                # Provides internationalization and localization support.
url_launcher: any                            # Allows opening URLs in external applications.
permission_handler: ^11.0.0                  # Handles runtime permissions for the app.
fluttertoast: any                            # Displays toast messages within the app.
flutter_spinkit: any                         # A collection of customizable loading indicators.
flutter_html: any                            # Parses and renders HTML in Flutter apps.
retrofit: ^4.4.1                             # Annotation-based HTTP client generator for Dart.
retrofit_generator: ^9.1.3                   # Generates Retrofit API implementation.
build_runner: ^2.4.13                        # Code generation tool used for tasks like building Retrofit files.